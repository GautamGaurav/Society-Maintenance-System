const SiteQuery = {
  insert: `INSERT INTO sites SET ?`,
  //VALUES  (?,  ?,    ?,       ?,             ?,       ?,    ?,     ?)`,
  select: "SELECT * FROM sites",
  update: "",
  delete: "",
};

export default SiteQuery;
